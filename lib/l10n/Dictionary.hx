package l10n;

/**
 * Словарь текстов.
 * Хеш мапа, содержит тексты (`String`) с ключом `Int32` для каждой отдельной фразы.
 * 
 * *Текстовые фразы в разных словарях должны иметь одинаковый ID.*
 */
typedef Dictionary = Dynamic;